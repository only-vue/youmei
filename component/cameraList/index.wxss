/* component/cameraList/index.wxss */
.camrea-view{
  margin:  20rpx 0 20rpx 0;
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}
.camrea-view .img-item{
  flex-grow: 1;
  position: relative;
}
.add-img{
 width: 200rpx;
 height: 200rpx;
}

.delete-img{
  position: absolute;
  right: 0;
  top: 0;
  width: 50rpx;
  height: 50rpx;
}